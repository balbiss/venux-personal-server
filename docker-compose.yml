version: '3.8'

services:
  venux-server:
    image: node:22-alpine
    restart: always
    working_dir: /app
    ports:
      - "8788:8788"
    command: >
      sh -c "echo 'Starting Connect deployment script v1.183...' &&
             apk add --no-cache git &&
             echo 'Cloning fresh repository (v1.183)...' &&
             rm -rf /app_tmp &&
             git clone https://github.com/balbiss/venux-personal-server.git /app_tmp &&
             echo 'Updating application files...' &&
             cp -rf /app_tmp/* . &&
             cp -rf /app_tmp/.* . 2>/dev/null || true &&
             echo 'Installing dependencies...' &&
             npm install --production &&
             echo 'Starting application v1.183...' &&
             mkdir -p /app/data &&
             touch /app/data/sessions.json &&
             ln -sf /app/data/sessions.json /app/sessions.json &&
             touch /app/data/bot.log &&
             ln -sf /app/data/bot.log /app/bot.log &&
             node server.mjs"
    # Adicionei PERSIST_STATE: "true" para manter o status entre deploys de 1.1.59-UI
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - PORT=${PORT:-8788}
      - WEBHOOK_URL=${WEBHOOK_URL}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - WUZAPI_BASE_URL=${WUZAPI_BASE_URL}
      - WUZAPI_ADMIN_TOKEN=${WUZAPI_ADMIN_TOKEN}
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
    # Portainer will manage env vars via its UI
    volumes:
      - venux_data:/app/bot_sessions

volumes:
  venux_data:
